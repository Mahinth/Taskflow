# Create a file called .env and copy these lines into it
# Replace the placeholder values with your own MongoDB Atlas credentials

MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/todolistDB?retryWrites=true&w=majority
PORT=3000
